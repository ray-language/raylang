// Bucle aritmético apretado: pila de operandos, saltos, aritmética entera.
fn main() -> int {
    var i: int = 0;
    var acc: int = 0;
    while (i < 10000000) {
        acc = acc + i * 2 - 1;
        i = i + 1;
    }
    acc % 1000
}
