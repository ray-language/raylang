[package]
name = "ssr-demo"
version = "0.1.0"
entry = "main.ray"

# El servidor web vive en el paquete `net` (tier 2, no embebido) → dependencia por ruta.
[dependencies]
net = "path:../../../packages/net"
