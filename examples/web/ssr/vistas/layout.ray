// GENERATED by `ray build --templates-only` from layout.ray.html — do NOT edit by hand;
// regenerate with `ray build --templates-only <path>`. The template is the source of truth.
from std/template import escape_html;

/// Renders the `layout` template (generated; the `.ray.html` file is the source of truth).
pub fn render_layout(titulo: string) -> string {
    var out: [string] = [];
    out.push("<!DOCTYPE html>\n<html>\n<head>\n    <meta charset=\"utf-8\">\n    <title>");
    out.push(escape_html(to_string(titulo)));
    out.push("</title>\n    <link rel=\"stylesheet\" href=\"/assets/app.css\">\n</head>\n<body>\n");
    out.push("\n");
    out.push("\n<footer>servido por raylang &middot; ");
    out.push(escape_html(to_string(titulo)));
    out.push("</footer>\n</body>\n</html>\n");
    out.join("")
}
