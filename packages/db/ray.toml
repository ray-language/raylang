# Manifiesto del paquete-librería `db` (sin entry; usa net/scram y net/time).
[package]
name = "db"
version = "0.1.2"
license = "Apache-2.0"
entry = "postgres.ray"

[dependencies]
net = "path:../net"
